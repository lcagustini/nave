#define MAX_OBJ_SIZE 1000

#define MAP_SCALE 0.4

#define MAP_SIZE 20
#define MAP_WORKERS 9
#define MAP_WORKTIME 60

#define PLAYER_ID 0

#define DEADZONE 25

#define GRAVITY 0.005f

#define max(a, b) ((a) > (b) ? (a) : (b))
#define min(a, b) ((a) < (b) ? (a) : (b))
